" Shahzad's NeoVim Specific Configurations (Guards for if nvim or vim).

" Configurations that can only be applied to nvim.
if has('nvim')

    " Begin nvim configurations.

endif

" Configurations that can only be applied to vim.
if !has('nvim')

    " Begin non-nvim configurations.

endif
